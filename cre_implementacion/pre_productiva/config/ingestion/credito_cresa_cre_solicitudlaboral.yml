config_version: 1
source_name: credito_cresa
source_schema: dbo
source_table: cre_solicitudlaboral
source_table_normalized: cre_solicitudlaboral
enabled: true

description: "Catálogo de configuración control solicitud desde CREDITO_CRESA. Ingesta a Bronze para consumo de dominios. Campos explicitados para ingesta incremental/validación."
priority:
  wave: fase_3_ingesta
  score: 5
  reason: "Tabla de maestro usada por procesos de normalización y reporting."

load_strategy:
  initial_mode: full_snapshot
  incremental_mode: full_snapshot
  watermark_column: null
  sequence_column: null
  delta_lookback_days: 0
  delete_detection: false

read:
  mode: table

primary_key:
  columns:
    id
  enforced_in_source: true

natural_key:
  columns:
    - id
    - solicitud_id
    - provincia_id
    - canton_id
    - parroquia_id
    - actividad_laboral_id
    - actividad_economica_id
    - relacion_dependencia_id
    - profesion_id

columns:
  include:
    - id
    - solicitud_id
    - provincia_id
    - canton_id
    - parroquia_id
    - actividad_laboral_id
    - actividad_economica_id
    - cargas_familiares
    - calle_principal
    - calle_transversal
    - nombre_empresa
    - salario
    - origen_ingresos
    - direccion
    - sector
    - referencia
    - tiempo_laboral
    - fecha_ingreso_laboral
    - telefono_fijo
    - cellular
    - relacion_dependencia_id
    - profesion_id
    - es_actual
    - actualizado
    - actualizado_por
    - creado
    - creado_por
    
  exclude: []
  rename: {}
  pii_columns: []
  restricted_columns: []

target:
  bronze_catalog: dlh_cresa
  bronze_schema: bronze
  bronze_table: credito_cresa_cre_solicitudlaboral
  write_mode_initial: overwrite
  write_mode_delta: merge
  merge_strategy: upsert_by_primary_key

audit_columns:
  add_ingestion_metadata: true
  add_record_hash: true
  add_schema_hash: true
  add_source_watermark: true

governance:
  domain_official: Maestro
  sensitivity: Interno
  data_owner: "Equipo de Datos - Maestro (por asignar oficialmente)"
  purview_collection: 01_Maestros

control_plane:
  run_table: dlh_cresa.audit01.gobierno_silver_reglas
  watermark_table: dlh_cresa.audit01.dynamics_odata_run_summary
  error_table: dlh_cresa.audit01.log_procesos
