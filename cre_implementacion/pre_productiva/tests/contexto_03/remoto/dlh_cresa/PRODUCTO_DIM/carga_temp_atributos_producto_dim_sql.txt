-- Databricks notebook source
--DROP TABLE IF EXISTS dlh_cresa.staging.temp_atributos_producto_dim;
--CREATE OR REPLACE TABLE IF NOT EXISTS

CREATE OR REPLACE TABLE dlh_cresa.staging.temp_atributos_producto_dim (
  atributo VARCHAR(60) COMMENT 'AttributeNam',
  cod_producto VARCHAR(100) COMMENT 'ProductNumber', 
  valor VARCHAR(60) COMMENT 'TextValue'
  --campo_x varchar(20) COMMENT '',  -- campos que se requieran crear
)
COMMENT 'Tabla temporal para procesos de construir dimension (temp_atributos_producto_dim)';





-- COMMAND ----------



CREATE OR REPLACE TEMPORARY VIEW carga_temp_atributos_producto_parquet
as 
select *from --parquet.`/Volumes/dlh_cresa/bronze/vol_bronze_dynamics/productattributevaluev2/productattributevaluev2.parquet`
dlh_cresa.bronze.ProductAttributeValuesV3;

--select *from carga_temp_atributos_producto_parquet;
--DESCRIBE TABLE carga_temp_atributos_producto_parquet;
--SHOW VIEWS;

TRUNCATE TABLE dlh_cresa.staging.temp_atributos_producto_dim;  

INSERT INTO dlh_cresa.staging.temp_atributos_producto_dim
select 
  AttributeName as atributo,
  ProductNumber	as cod_producto,
  TextValue	as valor
from carga_temp_atributos_producto_parquet;

