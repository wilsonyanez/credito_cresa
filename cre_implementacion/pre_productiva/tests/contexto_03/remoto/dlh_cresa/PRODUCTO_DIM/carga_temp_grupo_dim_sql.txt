-- Databricks notebook source
--DROP TABLE IF EXISTS dlh_cresa.staging.temp_grupo_dim;
--CREATE OR REPLACE TABLE IF NOT EXISTS

CREATE OR REPLACE TABLE dlh_cresa.staging.temp_grupo_dim (
  id_empresa VARCHAR(20) COMMENT 'dataAreaId',
  cod_grupo VARCHAR(20) COMMENT 'GroupCategory', 
  des_grupo VARCHAR(60) COMMENT 'APGroupCategoryName'
  --campo_x varchar(20) COMMENT '',  -- campos que se requieran crear
)
COMMENT 'Tabla temporal para procesos de construir dimension (temp_grupo_dim)';




-- COMMAND ----------


CREATE OR REPLACE TEMPORARY VIEW carga_temp_grupo_parquet
as 
select *from -- parquet.`/Volumes/dlh_cresa/bronze/vol_bronze_dynamics/commercialheirarchycategory/commercialheirarchycategory.parquet`
dlh_cresa.bronze.CommercialHeirarchyCategories;

--select *from carga_temp_grupo_parquet;
--DESCRIBE TABLE carga_temp_grupo_parquet;
--SHOW VIEWS;

TRUNCATE TABLE dlh_cresa.staging.temp_grupo_dim;  

INSERT INTO dlh_cresa.staging.temp_grupo_dim
select distinct
  dataAreaId as id_empresa,
  GroupCategory as cod_grupo,
  APGroupCategoryName as des_grupo
from carga_temp_grupo_parquet
where APGroupCategoryName IS NOT NULL;

